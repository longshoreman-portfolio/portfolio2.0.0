{
  "name": "portfolio",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "host":"npm run build && firebase emulators:start"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "firebase": "^9.8.2",
    "three": "^0.140.2"
  }
}
